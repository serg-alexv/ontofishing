# Antigravity (`agy`) role

Antigravity CLI is used as a local semantic/domain operator.

It may inspect CSVs and produce annotation JSON. It must not rewrite scoring logic or run applications automatically.

# Gemini CLI role

Gemini CLI is used as a semantic/domain driver over already-collected opportunities.

Preferred use:
- local authenticated CLI / Code Assist quota when available
- API key only when the user explicitly accepts that billing surface

Do not use Gemini CLI as the deterministic scorer.
